name: CodeRabbit Review

on:
  pull_request:
    branches: [main]

jobs:
  coderabbit:
    runs-on: ubuntu-latest
    steps:
      - uses: codeRabbitai/action@v1
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          github_app_token: ${{ secrets.CODERABBOT_APP_TOKEN }}